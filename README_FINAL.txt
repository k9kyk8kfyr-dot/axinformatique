AXinfo Ultimate FINAL — All Requested

Included:
- Premium dark Glassmorphism + Neon visual layer.
- Right-side navigation on desktop and right-side mobile drawer.
- Embedded Barcode / QR scanner inside the program (no separate scanner page).
- QR-only phone ↔ PC pairing flow; saved connection is reused for AutoSync.
- Product photo capture and local image retention.
- Purchase invoice photo capture and original-image retention.
- OCR for Arabic/French/English after first library load; OCR output is shown for review.
- Smart local matching of OCR text against existing product names/models/SKU/barcodes and stored sell price/stock.
- Sales, purchases, customers, debts/payments, repairs, expenses, reports, backup/restore and PC Bridge from the underlying AXinfo build.
- Multilingual UI already present in the underlying build.

Important:
- OCR and camera barcode libraries are loaded from jsDelivr on first use; the local database itself remains local/offline.
- Image-based product identification here is a local OCR + product-database matching assistant. A cloud vision AI service/API key is not bundled.
- For iPhone camera access, open AXinfo from a secure context (HTTPS) or an environment that grants camera permission.
